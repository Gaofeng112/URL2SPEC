# Local Config

This directory is for machine-local runtime configuration, especially browser
login state used by capture runs.

Examples:

- `vip.yaozh.com.storage_state.json`
- `cookies.json`
- `cookies.txt`

These files may contain private cookies, tokens, or account data and are ignored
by Git. Keep only non-sensitive documentation, such as this README, in version
control.
